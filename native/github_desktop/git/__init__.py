"""Git operations package."""

from .diff import format_discard_patch, format_partial_patch, parse_unified_diff, selectable_line_indices, side_by_side_rows
from .ops import (
    abort_cherry_pick,
    abort_merge,
    abort_rebase,
    add_remote,
    append_ignore_rule,
    apply_patch_to_index,
    checkout_branch,
    checkout_commit,
    checkout_paths,
    cherry_pick,
    clone_repository,
    co_author_trailers,
    continue_cherry_pick,
    continue_rebase,
    create_branch,
    create_commit,
    create_merge_commit,
    create_tag,
    delete_local_branch,
    delete_remote_branch,
    delete_tag,
    discard_changes_from_selection,
    discard_paths,
    ensure_repository,
    fetch,
    format_commit_message,
    get_ahead_behind,
    get_all_tags,
    get_author_identity,
    get_branches,
    get_changed_files,
    get_commit,
    get_commit_diff,
    get_commits,
    get_config_value,
    get_default_branch,
    get_merge_base,
    get_rebase_internal_state,
    get_remotes,
    get_stashes,
    get_status,
    get_submodules,
    get_working_directory_diff,
    init_repository,
    interactive_rebase_todo,
    is_lfs_repo,
    lfs_track,
    merge,
    pull,
    push,
    read_description,
    read_gitignore,
    rebase,
    remove_config_value,
    remove_remote,
    rename_branch,
    reorder_commits,
    reset,
    revert,
    rev_range,
    set_config_value,
    set_default_branch,
    set_remote_url,
    squash_commits,
    stage_files,
    stage_manual_resolution,
    stash_drop,
    stash_pop,
    stash_push,
    undo_commit,
    unstage_all,
    update_index,
    update_submodules,
    write_description,
    write_gitignore,
)
from .runner import env_for_remote, find_git, git, git_path_is_repository, resolve_repository_root
from .status import parse_porcelain_status

__all__ = [
    "git",
    "get_status",
    "create_commit",
    "clone_repository",
]
